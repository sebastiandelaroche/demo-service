{
  "name": "demo-service",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "start": "node ./src/server",
    "test": "mocha 'tests/**/*.spec.js' --exit",
    "docker-build": "docker build -t demo-service:latest .",
    "docker-run": "docker run  -p 3000:3000 -it demo-service:latest"
  },
  "dependencies": {
    "express": "^4.16.3"
  },
  "devDependencies": {
    "mocha": "^5.2.0"
  }
}